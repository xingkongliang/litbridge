__pycache__/
*.pyc
.env
.DS_Store
.streamlit/secrets.toml

# Top-level reference/ holds the React design mock (UI.ts) — not part of runtime.
/reference/

# Data directory — pre-indexed embeddings, papers, sample drafts. Generated at runtime.
data/
